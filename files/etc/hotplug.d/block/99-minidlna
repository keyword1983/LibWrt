[ "$ACTION" = "add" ] && [ "$DEVICE" = "sda2" ] && {
    logger -t minidlna-hotplug "External hard drive sda2 mounted, restarting minidlna after delay..."
    (sleep 3 && /etc/init.d/minidlna restart) &
}
