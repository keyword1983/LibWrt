[ "$ACTION" = "add" ] && [ "$DEVICE" = "sda2" ] && {
    logger -t minidlna-hotplug "External hard drive sda2 mounted, restarting minidlna after 8s delay..."
    (sleep 8 && /etc/init.d/minidlna restart) &
}
